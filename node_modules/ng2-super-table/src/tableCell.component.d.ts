import { ComponentResolver, OnInit, ViewContainerRef } from '@angular/core';
import { ColumnState } from './interfaces';
export declare class TableCell implements OnInit {
    private viewContainer;
    private resolver;
    row: any;
    column: ColumnState;
    cmpContainer: ViewContainerRef;
    constructor(viewContainer: ViewContainerRef, resolver: ComponentResolver);
    getValue(): any;
    getFormattedValue(): any;
    ngOnInit(): void;
}
