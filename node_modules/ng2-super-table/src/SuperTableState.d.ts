import { Observable } from 'rxjs/Observable';
import { ISuperTableColumn, ColumnState } from './interfaces';
export declare class SuperTableState {
    hasAnyFilters: boolean;
    columns: ColumnState[];
    sortStack: ColumnState[];
    stateChanged$: Observable<SuperTableState>;
    private stateChangedSource;
    constructor();
    setColumns(columns: Array<ISuperTableColumn>): void;
    toggleSort(colState: ColumnState, doNotClear: boolean): void;
    notify(): void;
}
