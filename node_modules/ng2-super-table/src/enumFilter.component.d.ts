import { OnInit, OnDestroy, ElementRef, ComponentResolver, ViewContainerRef } from '@angular/core';
import { ISuperTableFilter, ColumnState } from './interfaces';
import { SuperTableState } from './SuperTableState';
export declare class EnumFilter implements OnInit, OnDestroy {
    private state;
    private el;
    private viewContainer;
    private resolver;
    filter: ISuperTableFilter;
    column: ColumnState;
    private dropdown;
    private disabledChoices;
    private subscription;
    private disabledFilterCount;
    constructor(state: SuperTableState, el: ElementRef, viewContainer: ViewContainerRef, resolver: ComponentResolver);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private toggleVisibility();
}
