import { ElementRef } from '@angular/core';
import { ColumnState } from './interfaces';
import { SuperTableState } from './SuperTableState';
export declare class TableHeader {
    private el;
    private state;
    column: ColumnState;
    noHeight: boolean;
    constructor(el: ElementRef, state: SuperTableState);
    private getWidth();
    private getValue();
    private handleClick(event);
}
