import { ElementRef, SimpleChanges, OnChanges, OnInit, OnDestroy } from '@angular/core';
import { SuperTableState } from './SuperTableState';
export declare class SuperTableBody implements OnChanges, OnInit, OnDestroy {
    private el;
    private state;
    rows: Array<any>;
    tableClasses: any;
    bodyHeight: number;
    private visibleRows;
    private rowHeight;
    private rowOffset;
    private trackScroll;
    private onWindowResize;
    constructor(el: ElementRef, state: SuperTableState);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private updateVisibleRows();
    private detectRowHeight();
}
