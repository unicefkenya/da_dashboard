import { ISuperTableFilter, ColumnState } from './interfaces';
import { SuperTableState } from './SuperTableState';
export declare class TextFilter {
    private state;
    filter: ISuperTableFilter;
    column: ColumnState;
    onModelChange: Function;
    clearFilter(): void;
    constructor(state: SuperTableState);
}
